from django.contrib import admin
from django.urls import path
from . import views
urlpatterns = [
    #path('', web_view.index, name="home"),
    #path('<slug:slug>/', views.single_product, name="single_product"),
    #path('category/<cat:cat>/', views.category, name="category"),
]
